<!-- jumbotron -->
{{> header}}
<div class="container d-flex justify-content-between">
  <h1>My Properties</h1>
  <a href="/dashboard/add-property" class="p-3 bg-info">Add New Property</a>
</div>
<div class="container ">
  <div class="row flex">
    {{#each properties as |property|}}
      {{> property-dashboard}}
    {{/each}}
  </div>
</div>