<!-- Main jumbotron -->
<div class="jumbotron">
  <div class="nav">
    <div class="container">
      <div class="row">
        <div class="d-flex justify-content-between align-items-center">
          <img class="head-logo" src="/images/tenantively-logo-dark.png" alt="">
          <div class="d-flex menu-options align-items-center">
          <ul>
          {{#if loggedIn}}
            <li>Dashboard</li>
            <li>Logout</li>
        {{else}}
        <li>Log in</li>
        {{/if}}
          </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="container">
    <h1 class="display-3 mt-4">Manage your properties with ease.</h1>
  </div>
</div>